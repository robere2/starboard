{
  "name": "playground",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "[ -f index.ts ] && bun x nodemon -L --ext \"ts,json\" --watch . --watch ../../packages/api/dist/*.js --watch ../../packages/framework/dist/*.js --exec \"tsc && bun --inspect index.ts\""
  },
  "dependencies": {
    "@mcsb/api": "*",
    "eslint-config-custom": "*",
    "@mcsb/framework": "*",
    "tsconfig": "*"
  }
}
