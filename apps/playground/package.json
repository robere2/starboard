{
  "name": "playground",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "pg": "bun x nodemon -L --ext \"ts,json\" --exec \"tsc && bun --inspect index.ts\""
  },
  "dependencies": {
    "api": "*",
    "eslint-config-custom": "*",
    "framework": "*",
    "tsconfig": "*"
  }
}
