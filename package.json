{
  "name": "starboard",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun x nodemon -L --ext \"ts,json\" --exec \"tsc && bun --inspect playground.ts\"",
    "watch-test": "bun x nodemon -L --ext \"ts,json\" --exec \"tsc && bun --inspect test --coverage\""
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.2.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
