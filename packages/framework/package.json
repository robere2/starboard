{
  "name": "@mcsb/framework",
  "version": "0.0.0",
  "main": "dist/src/index.js",
  "types": "dist/src/index.js",
  "license": "MIT",
  "scripts": {
    "lint": "eslint . && madge --circular src",
    "test": "bun --inspect test --coverage",
    "test:watch": "bun x nodemon -L --ext \"ts,json\" --exec \"tsc && bun test\"",
    "dev": "bun run build",
    "build": "bun run types && esbuild `find src \\\\( -name '*.ts' \\\\)` --packages=external --outdir=dist/src --platform=node --sourcemap=external",
    "types": "tsc --build && echo \"Types built\""
  },
  "dependencies": {
    "@mcsb/api": "*"
  },
  "devDependencies": {
    "bun-types": "^1.0.6",
    "eslint-config-custom": "*",
    "tsconfig": "*",
    "typescript": "^5.0.0"
  }
}
