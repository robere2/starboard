{
  "name": "api",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "lint": "eslint .",
    "watch-test": "bun x nodemon -L --ext \"ts,json\" --exec \"tsc && bun --inspect test --coverage\""
  },
  "devDependencies": {
    "bun-types": "^1.0.6",
    "eslint-config-custom": "*",
    "tsconfig": "*",
    "typescript": "^5.0.0"
  }
}
